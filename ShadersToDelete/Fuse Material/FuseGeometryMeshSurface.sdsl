shader FuseGeometryMeshSurface : FuseGeometryMeshSurfaceStream
{
	// Helpers for working with mesh data in the format of pos, normals and uv
	MeshSurface makeSurface(float3 p, float3 n, float2 uv)
	{
		MeshSurface surface;
		surface.p = p;
		surface.n = n;
		surface.uv = uv;
		return surface;
	}
};