shader FuseCommonTypes
{

	struct Matrix4x4
	{
		float4x4 Matrix;
	};
	/*
	struct Ray
	{
		float3 ro, rd, sp, sn; // ray orign, ray direction, surface position, surface normal
	};

	Ray makeRay(float3 ro, float3 rd, float3 sp = 0.0, float3 sn = 0.0)
	{
		Ray ray;
		ray.ro = ro;
		ray.rd = rd;
		ray.sp = sp;
		ray.sn = sn;
		return  ray;
	}
	*/
	

};