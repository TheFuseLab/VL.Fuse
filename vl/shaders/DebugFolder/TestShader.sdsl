
shader TestShader
{
    #define MYVALUE 1
    
    // see TestShader2, for deme of preprocessor not working in other shader that uses this one (Actully does work, but the stride plugin calles an error) 

    float foo()
    {
        return .5;
    }
};